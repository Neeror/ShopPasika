"use client";import {useState} from 'react';import Shell from '@/components/Shell';const qs=[['Як швидко відправляють?','Зазвичай протягом 1-2 робочих днів.'],['Як повернути товар?','Напишіть продавцю протягом 14 днів.'],['Чи перевіряєте продавців?','Так, перевіряємо документи пасіки та адресу.']];export default function Help(){const [o,setO]=useState(-1);return <Shell><main className="wrap py-14"><h1 className="text-4xl">Допомога</h1><section className="mt-8 max-w-2xl divide-y divide-line rounded-2xl border border-line">{qs.map(([q,a],i)=><div className="p-5" key={q}><button className="w-full text-left font-semibold" onClick={()=>setO(o===i?-1:i)}>{q}</button>{o===i&&<p className="mt-3 text-ink/65">{a}</p>}</div>)}</section></main></Shell>}
